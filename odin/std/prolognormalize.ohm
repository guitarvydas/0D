prolognormalize {
  input = item+
  item =
    | string -- string
    | space -- space
    | integer -- integer
    | word -- word

  word = (letter | "_") legalchar+
  legalchar = letter | digit | "_"

  integer = digit+
  pctcomment = "%" (~"\n" any)* "\n"
  comment = pctcomment
  space += comment
  string = dq (~dq any)* dq
  dq = "\""
}
