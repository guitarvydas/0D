lispnormalize {
  input = item+
  item =
    | string -- string
    | space -- space
    | integer -- integer
    | word -- word

  word = (letter | "_") legalchar+
  legalchar = letter | digit | "_"

  integer = digit+
  lispcomment = ";" (~"\n" any)* "\n"
  comment = lispcomment
  space += comment
  string = dq (~dq any)* dq
  dq = "\""
}
