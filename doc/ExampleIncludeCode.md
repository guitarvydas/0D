to include Components in the project...

full spec:
    zd.append_leaf (leaves, zd.Leaf_Template { name = "Echo", instantiate = echo })

string:
    zd.append_leaf (leaves, std.string_constant ("Arithmetic"))
